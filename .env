# Perplexity MCP 服务器环境变量配置示例
# 复制此文件为 .env 并设置相应的值

# ======================
# Perplexity API 配置
# ======================

# Perplexity API 密钥 (必需)
# 从 https://perplexity.ai 获取
PERPLEXITY_API_KEY=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

# 使用的模型名称 (可选，默认: sonar)
# 可选值: sonar, sonar-pro, codellama-34b-instruct, llama-2-70b-chat
PERPLEXITY_MODEL=sonar

# 默认系统消息 (可选)
PERPLEXITY_SYSTEM_MESSAGE=Be precise and concise. Provide accurate information with citations.

# API 请求超时时间，单位：秒 (可选，默认: 30.0)
PERPLEXITY_TIMEOUT=30.0

# 最大重试次数 (可选，默认: 3)
PERPLEXITY_MAX_RETRIES=3

# 重试延迟时间，单位：秒 (可选，默认: 1.0)
PERPLEXITY_RETRY_DELAY=1.0

# ======================
# MCP 服务器配置
# ======================

# 服务器绑定地址 (可选，默认: 127.0.0.1)
# 使用 0.0.0.0 允许外部访问
MCP_HOST=0.0.0.0

# 服务器端口 (可选，默认: 8000)
MCP_PORT=8000

# MCP 端点路径 (可选，默认: /mcp)
MCP_PATH=/mcp

# ======================
# 日志和调试配置
# ======================

# 日志级别 (可选，默认: info)
# 可选值: debug, info, warning, error, critical
LOG_LEVEL=info

# 是否启用调试模式 (可选，默认: false)
# 启用后会显示更详细的错误信息
DEBUG=false

# ======================
# 代理配置 (可选)
# ======================

# HTTP 代理配置
# 基本代理格式
# HTTP_PROXY=http://proxy.company.com:7897
# HTTPS_PROXY=https://proxy.company.com:7897

# 带认证的代理格式
# HTTP_PROXY=http://username:password@proxy.company.com:7897
# HTTPS_PROXY=https://username:password@proxy.company.com:7897

# 通用代理 (当未设置具体协议代理时使用)
ALL_PROXY=http://127.0.0.1:7897

# 不使用代理的地址列表 (逗号分隔)
# NO_PROXY=localhost,127.0.0.1,.local

# SOCKS 代理支持
# HTTP_PROXY=socks5://proxy.company.com:1080
# HTTPS_PROXY=socks5://proxy.company.com:1080

# ======================
# 高级配置
# ======================

# 是否启用 CORS (可选，默认: true)
ENABLE_CORS=true

# 最大请求大小，单位：字节 (可选，默认: 1048576 = 1MB)
MAX_REQUEST_SIZE=1048576